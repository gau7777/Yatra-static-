<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yatra">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditDestActivity"></activity>
        <activity android:name=".EditPlaceActivity" />
        <activity android:name=".AddDestinationActivity" />
        <activity
            android:name=".AdminDestListActivity"
            android:label="@string/title_activity_admin_dest_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddPlaceActivity" />
        <activity
            android:name=".AdminPlaceViewActivity"
            android:label="@string/title_activity_admin_place_view"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".GuestDestDetailsActivity" />
        <activity android:name=".GuestDestListActivity" />
        <activity
            android:name=".MainActivity"
            android:label="Yatra"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminLoginActivity" />
    </application>

</manifest>